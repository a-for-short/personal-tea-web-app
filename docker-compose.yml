services:
  tea:
    build: .
    container_name: tea-app
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    ports:
      - "127.0.0.1:5000:5000"
    environment:
      - FLASK_DEBUG=0
      - PORT=5000
      - SECRET_KEY=${SECRET_KEY:-default-secret-change-me}
